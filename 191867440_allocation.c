#include<stdlib.h>
#include<string.h>
#include<stdio.h>

void main()

{
    int bSize[15], pSize[15], throws[15], allocation[15];
    int i, j;
    int processNumber, blockNumber;

    for(i = 0; i < 10; i++) {
        throws[i] = 0;
        allocation[i] = -1;
    }
    printf("Allocation amount:  ");
    scanf("%d", &blockNumber);
    printf("\nEnter size: ");

}